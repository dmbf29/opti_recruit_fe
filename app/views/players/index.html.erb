<div class="container mt-5">
  <%= render 'header' %>
  <div class="d-flex justify-content-between align-items-center">
    <%= render 'tabs', active: 'squads' %>
    <%= render 'positions' %>
  </div>
  <div class="row mt-4  ">
    <div class="col">
      <%= render 'players/tables', players: @players %>
    </div>
  </div>
</div>
