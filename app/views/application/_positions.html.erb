<div class="btn-group">
  <%= link_to 'All', players_path(team_id: @team.id), class: "btn border-light #{'btn-secondary active' if params['position'].blank? }" %>
  <%= link_to 'FW', players_path(team_id: @team.id, position: 'FW'), class: "btn border-light #{'btn-secondary active' if params['position'] == 'FW'}" %>
  <%= link_to 'MF', players_path(team_id: @team.id, position: 'MF'), class: "btn border-light #{'btn-secondary active' if params['position'] == 'MF'}" %>
  <%= link_to 'DF', players_path(team_id: @team.id, position: 'DF'), class: "btn border-light #{'btn-secondary active' if params['position'] == 'DF'}" %>
  <%= link_to 'GK', players_path(team_id: @team.id, position: 'GK'), class: "btn border-light #{'btn-secondary active' if params['position'] == 'GK'}" %>
</div>
